import React from "react";

const ErrorBoundary: React.FC = () => {
    return (
        <div>
            <p className="text-red">TESTE</p>error
        </div>
    );
};

export default ErrorBoundary;
