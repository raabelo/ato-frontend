// const actions = (): string[] => {
//     card.text?.split(":");
//     console.table(card.text?.split(":"));
//     return [];
//     // .map((text) => {
//     //     text.split("\n");
//     // });
//     return [];

// {actions().map((action) => (
//     <CardButton title={action} action={() => {}} />
// ))}
// };
